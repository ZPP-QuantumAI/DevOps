on:
  workflow_call:
jobs:
  build-docker:
    runs-on: ubuntu-latest
    name: 'docker build'
    steps:
      - secrets:
        DOCKER_KEY: ${{ secrets.DOCKER_KEY }}
      - uses: ZPP-QuantumAI/DevOps/actions/build@main
