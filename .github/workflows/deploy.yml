on:
  workflow_call:
jobs:
  deploy:
    runs-on: ubuntu-latest
    name: 'deploy'
    steps:
      - name: Test
        env:
          TEST: ${{ secrets.TEST }}
        run: |
          echo $TEST
        shell: bash
