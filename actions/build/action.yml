name: 'Docker build'
description: 'Builds docker images'
runs:
  using: composite
  steps:
    - uses: actions/checkout@v3
    - run: |
        wget https://raw.githubusercontent.com/ZPP-QuantumAI/DevOps/build.sh
        ls
        chmod +x build.sh
        ./build.sh ${{ github.event.repository.name }}
      shell: bash
