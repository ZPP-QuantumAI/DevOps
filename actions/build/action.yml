name: 'Docker build'
description: 'Builds docker images'
runs:
  using: composite
  steps:
#    - uses: actions/checkout@v3
#      env:
#        DOCKER_KEY: ${{ inputs.docker_key }}
#    - run: echo $DOCKER_KEY
#      shell: bash
    - uses: docker/login-action@v2.1.0
#      env:
#        DOCKER_KEY: ${{ inputs.docker_key }}
      with:
        registry: ghcr.io
        username: mmaciek87
        password: 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
    - uses: actions/checkout@v3
    - run: |
        wget https://raw.githubusercontent.com/ZPP-QuantumAI/DevOps/main/build.sh
        chmod +x build.sh
        ./build.sh ${{ github.event.repository.name }}
      shell: bash
